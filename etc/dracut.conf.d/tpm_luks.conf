# Include the necessary modules
add_dracutmodules+=" systemd crypt tpm2-tss "

# Ensure the kernel drivers for TPM are included (often done by hostonly, but good to be explicit)
# If your TPM driver is not included automatically, you might need to find it and add it here.
# E.g., add_drivers+=" tpm_crb tpm_tis "
